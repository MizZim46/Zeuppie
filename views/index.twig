{% extends layout %}


{% block body %}
        <section id="content">
          <section class="vbox">
            <section class="scrollable wrapper-lg">
              <div class="row">

                {% for art in articles %}

                              <div class="col-sm-4">
                                <div class="blog-post">
                                  <div class="post-item">
                                    <div class="caption wrapper-lg">
                                      <h2 class="post-title"><a href="#">{{ art.titre|title }}</a></h2>
                                      <div class="post-sum">
                                        <p>
                                          {{ art.contenu|truncate(250, true, '...')|raw }}
                                        </p>
                                      </div>
                                      <div class="line line-lg"></div>
                                      <div class="text-muted">
                                        <i class="fa fa-user icon-muted"></i> by <a href="#" class="m-r-sm">{{ art.pseudo }}</a>
                                        <i class="fa fa-folder-open icon-muted"></i> <a href="#" class="m-r-sm">{{ art.nom_categories }}</a>
                                        <i class="fa fa-clock-o icon-muted"></i> {{ art.date_articles }}
                                        <a href="#" class="m-l-sm"><i class="fa fa-comment-o icon-muted"></i> 4 comments</a>
                                      </div>
                                    </div>
                                  </div>
                                </div>
                              </div>

                {% endfor %}

                            </div>

          </section>
        </section>
      </section>

{% endblock %}